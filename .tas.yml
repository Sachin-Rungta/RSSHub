framework: jest
preRun:
     command:
        - yarn install
preMerge:
  pattern:
    - "./test/**/*.js"
    - "./test/*.js"
postMerge:
  pattern: 
    - "./test/utils/common-config.js"
#     - "./test/*.js"
postRun:
     command:
        - yarn --version
version: 1.0
